<%= form_with(model: larderdb, local: true) do |form| %>
  <% if larderdb.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(larderdb.errors.count, "error") %> prohibited this larderdb from being saved:</h2>

      <ul>
      <% larderdb.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :food %>
    <%= form.text_field :food %>
  </div>

  <div class="field">
    <%= form.label :nutrition %>
    <%= form.text_area :nutrition %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
